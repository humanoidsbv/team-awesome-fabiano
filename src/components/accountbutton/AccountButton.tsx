import * as Styled from "./AccountButton.styled";

export const AccountButton = (): React.ReactElement => {
  return (
    <Styled.Container>
      <Styled.ProfilePic />
    </Styled.Container>
  );
};
